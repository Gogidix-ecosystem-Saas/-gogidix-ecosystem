server:
  port: 8890
  servlet:
    context-path: /api/v1

spring:
  application:
    name: environment-config-service

  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/gogidix_environment_config
    username: ${DB_USERNAME:gogidix_user}
    password: ${DB_PASSWORD:gogidix_pass}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  redis:
    host: localhost
    port: 6379
    database: ${REDIS_DB:1}

  security:
    user:
      name: ${SECURITY_USERNAME:admin}
      password: ${SECURITY_PASSWORD:admin}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    export:
      prometheus:
        enabled: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.gogidix.centralconfiguration.environmentconfig: INFO
  file:
    name: logs/environment-config-service.log

# Enterprise Environment Configuration Settings
gogidix:
  environment-config:
    encryption:
      enabled: true
      algorithm: AES-256
    approval:
      required: true
      minimum-approvers: 2
    versioning:
      enabled: true
      max-versions: 50
    compliance:
      audit-trail: true
      change-notifications: true

---
spring:
  profiles: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/gogidix_environment_config_dev

---
spring:
  profiles: prod
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD}
